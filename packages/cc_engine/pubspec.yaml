name: cc_engine
description: Расчётный движок «Калькулятора проектировщика». Pure Dart, без Flutter — расчёт нагрузок, подбор сечений, армирование, теплотехника по СП.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.10.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
